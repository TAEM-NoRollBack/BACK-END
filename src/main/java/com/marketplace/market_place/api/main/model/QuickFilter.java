package com.marketplace.market_place.api.main.model;

public enum QuickFilter {
    NEARBY,   // 내 주변
    LUNCH,    // 점심식사(임시: 반경 기준)
    NEW,      // 신규 맛집(임시: 최신 id 등으로 대체)
    EULJI,    // 을지대 주변
    ALL       // 전체
}